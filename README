INTRODUCTION
------------

  Tracemalloc is a tool used to detect memory leaks caused by 'malloc' call.It generates report about each leaked allocation which gives details such as bytes leaked , file name, function name, line number etc.

BUILD
-----

type "make".This will build the static library(libtracemalloc.a) and excutable (tracemalloc).


USAGE
-----

tracemalloc <build command>

eg: tracemalloc gcc test.c

